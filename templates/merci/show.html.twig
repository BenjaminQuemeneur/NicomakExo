{% extends 'base.html.twig' %}

{% block title %}Merci{% endblock %}

{% block body %}
    <h1>Merci</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ merci.id }}</td>
            </tr>
            <tr>
                <th>Merci</th>
                <td>{{ merci.Merci }}</td>
            </tr>
            <tr>
                <th>Expediteur</th>
                <td>{{ merci.expediteur }}</td>
            </tr>
            <tr>
                <th>Destinataire</th>
                <td>{{ merci.destinataire }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ merci.message }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ merci.date ? merci.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_merci_index') }}">back to list</a>

    <a href="{{ path('app_merci_edit', {'id': merci.id}) }}">edit</a>

    {{ include('merci/_delete_form.html.twig') }}
{% endblock %}
